# Motor Beta 1 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Levar o motor de alpha (autor alimenta JSON limpo, conhece os trilhos) a beta-1 (um estranho roda o happy-path e nada quebra silenciosamente), e listar a rota de pesquisa pós-beta sem fabricá-la.

**Architecture:** Duas fases. **Fase A (beta, Tasks 1–8)** = código determinístico testável: validação de contrato LLM↔código, mecanismo de histórico real, dados do floor-plan, runner CLI, e os testes que faltam (cicatriz e2e, embed, lentes). **Fase B (pesquisa, Tasks 9–13)** = itens `[Especulativo]` ainda não especificados; cada um começa por um **design-spike** porque escrever código-completo deles agora seria invenção — o pecado que o próprio motor mata.

**Tech Stack:** Python 3.13 (stdlib only — `json`, `csv`, `pathlib`, `sys`; sem deps novas), pytest. Agentes são prompts Claude Code (não testáveis em unit; cobertos por fixtures de contrato). Dados por projeto em `projetos/NN/dados/*.json`.

**Princípio lazy:** stdlib > lib; sem schema-framework (validação à mão em ~40 linhas); os agentes-prompt não viram código. Fase B não infla o escopo do beta — só sai se o usuário fundar escopo explicitamente.

---

## File Structure

| Arquivo | Responsabilidade | Task |
|---|---|---|
| `engine/contract.py` (novo) | Valida shape + vocabulário fechado dos JSON que o motor consome (claims, verdicts, floor-plan). Falha-alto, nunca silencioso. | 1, 6 |
| `engine/journal.py` (novo) | Histórico real: append/read de `decision-log.md` + `addendum.md`; leitura de `contexto.md`. | 2, 3 |
| `engine/run.py` (novo) | CLI: roda a cadeia determinística sobre um `projetos/NN/` e reporta o estado. Entrypoint do beta-tester. | 4 |
| `tests/test_contract.py` (novo) | Cobre contract.py. | 1, 6 |
| `tests/test_journal.py` (novo) | Cobre journal.py. | 2, 3 |
| `tests/test_run.py` (novo) | Cobre o runner. | 4 |
| `tests/test_e2e.py` (novo) | Cadeia ponta-a-ponta determinística + **cicatriz e2e** (reabre pai → re-cicatriza filhos). | 5 |
| `tests/test_embed.py` (novo) | Unit de `engine/embed.py` (único módulo sem teste). | 7 |
| `tests/test_lentes.py` (novo) | `engine/lentes.csv` parseia + shape. | 8 |
| `README.md` (modificar) | Quickstart: como um estranho roda um ciclo. | 4 |
| `.claude/agents/meta-analista.md` (modificar) | Emitir `floor-plan.json` além do `.md`. | 3 |

---

# FASE A — BETA

## Task 1: `engine/contract.py` — validar claims + verdicts

**Files:**
- Create: `engine/contract.py`
- Test: `tests/test_contract.py`

A costura que quebra quando outro roda: o motor consome JSON de agente e nada checa a forma. Vocabulário real (do código e dos prompts):
- claim: `id`(str, obrigatório), `content`(str), `binding`∈{`vinculante`,`ilustrativo`,`default-sobrescrevivel`}, `provenance`, `parent`(str|null), `links`(list). Campos opcionais: `rationale`, `category`, `domain`, `derives_from`, `parent_constraint`, `derivation`, `provisional`, `stale`.
- verdict: `id`(str), `status`∈os SEIS, `structural_gaps`(list[str]), `verification_items`(list[obj]), `elicitation_question`(str|null), `contradiction`(str|null). Opcionais: `resolved_gaps`, `verified_items`.
- verification_item: `text`(str), `domain`∈{`lei`,`engenharia`,`financeiro`,null}, `critical`(bool). Opcional: `candidates`.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_contract.py
import pytest
from engine.contract import (
    SIX_STATUS, validate_claim, validate_verdict, validate_claims, validate_verdicts,
    ContractError,
)

def test_six_status_fechado():
    assert SIX_STATUS == {
        "CONTRADIZ-NIVEL-ACIMA", "PRECISA-JUSTIFICAR", "INCOMPLETO",
        "PENDENTE-VERIFICAÇÃO", "CONTESTADO", "PASSA",
    }

def test_verdict_status_invalido_falha():
    with pytest.raises(ContractError, match="status"):
        validate_verdict({"id": "x", "status": "INVENTADO", "structural_gaps": [],
                          "verification_items": [], "elicitation_question": None,
                          "contradiction": None})

def test_verdict_valido_passa():
    v = {"id": "x", "status": "PASSA", "structural_gaps": [],
         "verification_items": [], "elicitation_question": None, "contradiction": None}
    assert validate_verdict(v) == v

def test_verification_item_domain_invalido_falha():
    v = {"id": "x", "status": "PENDENTE-VERIFICAÇÃO", "structural_gaps": [],
         "verification_items": [{"text": "q?", "domain": "astrologia", "critical": True}],
         "elicitation_question": None, "contradiction": None}
    with pytest.raises(ContractError, match="domain"):
        validate_verdict(v)

def test_elicitation_obrigatoria_quando_precisa_justificar():
    v = {"id": "x", "status": "PRECISA-JUSTIFICAR", "structural_gaps": [],
         "verification_items": [], "elicitation_question": None, "contradiction": None}
    with pytest.raises(ContractError, match="elicitation"):
        validate_verdict(v)

def test_claim_binding_invalido_falha():
    with pytest.raises(ContractError, match="binding"):
        validate_claim({"id": "x", "content": "c", "binding": "talvez",
                        "provenance": "human decision", "parent": None, "links": []})

def test_claim_id_ausente_falha():
    with pytest.raises(ContractError, match="id"):
        validate_claim({"content": "c", "binding": "vinculante",
                        "provenance": "human decision", "parent": None, "links": []})

def test_validate_lista_acumula_erros():
    bad = [{"id": "a", "status": "PASSA", "structural_gaps": [], "verification_items": [],
            "elicitation_question": None, "contradiction": None},
           {"id": "b", "status": "NOPE", "structural_gaps": [], "verification_items": [],
            "elicitation_question": None, "contradiction": None}]
    with pytest.raises(ContractError, match="b"):
        validate_verdicts(bad)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m pytest tests/test_contract.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'engine.contract'`

- [ ] **Step 3: Write minimal implementation**

```python
# engine/contract.py
"""Contrato LLM↔código. Valida o shape + vocabulário fechado dos JSON que o motor
consome. Falha-alto (ContractError), nunca silencioso — o LLM erra a forma, aqui pega.
Sem schema-framework: validação à mão, stdlib só."""

SIX_STATUS = {
    "CONTRADIZ-NIVEL-ACIMA", "PRECISA-JUSTIFICAR", "INCOMPLETO",
    "PENDENTE-VERIFICAÇÃO", "CONTESTADO", "PASSA",
}
BINDINGS = {"vinculante", "ilustrativo", "default-sobrescrevivel"}
DOMAINS = {"lei", "engenharia", "financeiro", None}


class ContractError(ValueError):
    pass


def _require(cond, msg):
    if not cond:
        raise ContractError(msg)


def validate_claim(c):
    _require(isinstance(c.get("id"), str) and c["id"], f"claim sem id válido: {c!r}")
    _require(isinstance(c.get("content"), str), f"claim {c['id']}: content ausente")
    _require(c.get("binding") in BINDINGS,
             f"claim {c['id']}: binding inválido {c.get('binding')!r}")
    _require("links" in c and isinstance(c["links"], list),
             f"claim {c['id']}: links deve ser lista")
    return c


def validate_verdict(v):
    cid = v.get("id")
    _require(isinstance(cid, str) and cid, f"verdict sem id válido: {v!r}")
    _require(v.get("status") in SIX_STATUS,
             f"verdict {cid}: status fora dos seis: {v.get('status')!r}")
    _require(isinstance(v.get("structural_gaps"), list), f"verdict {cid}: structural_gaps")
    items = v.get("verification_items")
    _require(isinstance(items, list), f"verdict {cid}: verification_items deve ser lista")
    for it in items:
        _require(isinstance(it.get("text"), str), f"verdict {cid}: item sem text")
        _require(it.get("domain") in DOMAINS, f"verdict {cid}: domain inválido {it.get('domain')!r}")
        _require(isinstance(it.get("critical"), bool), f"verdict {cid}: critical deve ser bool")
    if v["status"] == "PRECISA-JUSTIFICAR":
        _require(isinstance(v.get("elicitation_question"), str) and v["elicitation_question"],
                 f"verdict {cid}: elicitation_question obrigatória em PRECISA-JUSTIFICAR")
    if v["status"] == "CONTRADIZ-NIVEL-ACIMA":
        _require(v.get("contradiction"), f"verdict {cid}: contradiction obrigatória em CONTRADIZ")
    return v


def validate_claims(claims):
    for c in claims:
        validate_claim(c)
    return claims


def validate_verdicts(verdicts):
    for v in verdicts:
        validate_verdict(v)
    return verdicts
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m pytest tests/test_contract.py -q`
Expected: PASS (8 passed)

- [ ] **Step 5: Commit**

```bash
git add engine/contract.py tests/test_contract.py
git commit -m "feat(contract): validação do contrato LLM↔código (seis status + shapes)"
```

---

## Task 2: `engine/journal.py` — decision-log + addendum (histórico real)

**Files:**
- Create: `engine/journal.py`
- Test: `tests/test_journal.py`

Hoje os 3 docs são só prosa no orchestrator (dívida de honestidade do commit `8f88104`). Vira mecanismo: append na voz humana, append-only, reversão = linha nova.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_journal.py
from engine.journal import log_decision, read_decisions, append_addendum, read_context

def test_log_decision_anexa_linha(tmp_path):
    p = tmp_path / "decision-log.md"
    log_decision(str(p), nivel="ideacao", decisao="claim 3 vinculante", porque="é a lei do projeto", data="2026-06-23")
    log_decision(str(p), nivel="ideacao", decisao="desce p/ concepção (provisional)", porque="sentir movimento", data="2026-06-23")
    linhas = read_decisions(str(p))
    assert len(linhas) == 2
    assert "claim 3 vinculante" in linhas[0]
    assert linhas[0].startswith("- [2026-06-23]")

def test_log_decision_append_only_nao_reescreve(tmp_path):
    p = tmp_path / "decision-log.md"
    log_decision(str(p), nivel="x", decisao="A", porque="r1", data="2026-06-23")
    antes = p.read_text(encoding="utf-8")
    log_decision(str(p), nivel="x", decisao="reverte A", porque="novo fato", data="2026-06-23")
    assert p.read_text(encoding="utf-8").startswith(antes)  # nada apagado

def test_porque_ausente_fica_visivel_nao_inventado(tmp_path):
    p = tmp_path / "decision-log.md"
    log_decision(str(p), nivel="x", decisao="A", porque=None, data="2026-06-23")
    assert "— (sem porquê registrado)" in read_decisions(str(p))[0]

def test_addendum_append(tmp_path):
    p = tmp_path / "addendum.md"
    append_addendum(str(p), texto="pensei em cobrar por uso, mas não", data="2026-06-23")
    assert "cobrar por uso" in p.read_text(encoding="utf-8")

def test_read_context_ausente_retorna_vazio(tmp_path):
    assert read_context(str(tmp_path / "contexto.md")) == ""

def test_read_context_le_fatos(tmp_path):
    p = tmp_path / "contexto.md"
    p.write_text("projeto jurídico: fato sem excerpt = crítico\n", encoding="utf-8")
    assert "jurídico" in read_context(str(p))
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m pytest tests/test_journal.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'engine.journal'`

- [ ] **Step 3: Write minimal implementation**

```python
# engine/journal.py
"""Histórico real (o 'porquê' humano), complementar ao events.jsonl (máquina).
- decision-log.md: toda decisão/override humana, append-only, voz humana.
- addendum.md: profundidade que o claim dropa (alternativa rejeitada, qualitativo).
- contexto.md: fatos/restrições estáticos do projeto (read-only aqui).
Reversão = linha nova, nunca reescrita. Porquê ausente = visível, não inventado."""
import os


def log_decision(path, *, nivel, decisao, porque, data):
    razao = porque if porque else "(sem porquê registrado)"
    linha = f"- [{data}] {nivel} · {decisao} — {razao}\n"
    with open(path, "a", encoding="utf-8") as f:
        f.write(linha)
    return linha


def read_decisions(path):
    if not os.path.exists(path):
        return []
    with open(path, "r", encoding="utf-8") as f:
        return [ln.rstrip("\n") for ln in f if ln.startswith("- [")]


def append_addendum(path, *, texto, data):
    linha = f"- [{data}] {texto}\n"
    with open(path, "a", encoding="utf-8") as f:
        f.write(linha)
    return linha


def read_context(path):
    if not os.path.exists(path):
        return ""
    with open(path, "r", encoding="utf-8") as f:
        return f.read()
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m pytest tests/test_journal.py -q`
Expected: PASS (6 passed)

- [ ] **Step 5: Commit**

```bash
git add engine/journal.py tests/test_journal.py
git commit -m "feat(journal): histórico real (decision-log + addendum + contexto)"
```

---

## Task 3: floor-plan como dado + meta-analista emite JSON

**Files:**
- Create: `engine/floorplan.py`
- Modify: `.claude/agents/meta-analista.md` (instruir a emitir `floor-plan.json` além do `.md`)
- Test: `tests/test_floorplan.py`

O `floor-plan.md` é prosa (não testável). A cidade precisa de dado. Solução: o meta-analista emite também `floor-plan.json` validável; `floorplan.py` valida + a viz lê.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_floorplan.py
import pytest
from engine.floorplan import validate_floorplan, FloorplanError, ANDAR_STATUS

def test_floorplan_valido():
    fp = {"objetivo": "simulador de caixa", "objetivo_confirmado": True,
          "diagnostico": "no-caminho",
          "andares": [{"nivel": "ideacao", "status": "cheio"},
                      {"nivel": "concepcao", "status": "provisional"},
                      {"nivel": "codigo", "status": "vazio"}]}
    assert validate_floorplan(fp) == fp

def test_diagnostico_fechado():
    fp = {"objetivo": "x", "objetivo_confirmado": False, "diagnostico": "voando",
          "andares": []}
    with pytest.raises(FloorplanError, match="diagnostico"):
        validate_floorplan(fp)

def test_andar_status_fechado():
    fp = {"objetivo": "x", "objetivo_confirmado": True, "diagnostico": "girando",
          "andares": [{"nivel": "ideacao", "status": "meio-cheio"}]}
    with pytest.raises(FloorplanError, match="status"):
        validate_floorplan(fp)
    assert ANDAR_STATUS == {"cheio", "provisional", "vazio"}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m pytest tests/test_floorplan.py -q`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: Write minimal implementation**

```python
# engine/floorplan.py
"""Dado do floor-plan (o .md é prosa; este é o que a cidade lê). Valida shape."""
ANDAR_STATUS = {"cheio", "provisional", "vazio"}
DIAGNOSTICO = {"no-caminho", "girando", "pulando"}


class FloorplanError(ValueError):
    pass


def validate_floorplan(fp):
    if fp.get("diagnostico") not in DIAGNOSTICO:
        raise FloorplanError(f"diagnostico fora do fechado: {fp.get('diagnostico')!r}")
    if not isinstance(fp.get("objetivo"), str):
        raise FloorplanError("objetivo ausente")
    for a in fp.get("andares", []):
        if a.get("status") not in ANDAR_STATUS:
            raise FloorplanError(f"andar status inválido: {a.get('status')!r}")
    return fp
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m pytest tests/test_floorplan.py -q`
Expected: PASS (3 passed)

- [ ] **Step 5: Modify meta-analista.md** — adicionar à seção de gravação do floor-plan:

```markdown
Além do `dados/floor-plan.md` (prosa), emita `dados/floor-plan.json` com o shape:
`{objetivo, objetivo_confirmado, diagnostico ∈ {no-caminho,girando,pulando},
andares:[{nivel, status ∈ {cheio,provisional,vazio}}]}`. É o que a cidade lê.
```

- [ ] **Step 6: Commit**

```bash
git add engine/floorplan.py tests/test_floorplan.py .claude/agents/meta-analista.md
git commit -m "feat(floorplan): dado validável do floor-plan + meta-analista emite json"
```

---

## Task 4: `engine/run.py` — CLI runner (entrypoint do beta-tester)

**Files:**
- Create: `engine/run.py`
- Modify: `README.md`
- Test: `tests/test_run.py`

Um estranho precisa rodar e ver o estado. O runner roda a cadeia determinística sobre `projetos/NN/` e reporta: contrato OK?, scores, gate de descida, integridade.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_run.py
import json
from engine.run import run_project

def _mk(tmp_path):
    d = tmp_path / "dados"; d.mkdir()
    (d / "claims.json").write_text(json.dumps([
        {"id": "a", "content": "c", "binding": "vinculante",
         "provenance": "human decision", "parent": None, "links": []}
    ]), encoding="utf-8")
    (d / "verdicts.json").write_text(json.dumps([
        {"id": "a", "status": "PASSA", "structural_gaps": [], "verification_items": [],
         "elicitation_question": None, "contradiction": None}
    ]), encoding="utf-8")
    return tmp_path

def test_run_projeto_valido_reporta_ok(tmp_path):
    proj = _mk(tmp_path)
    rep = run_project(str(proj))
    assert rep["contract_ok"] is True
    assert rep["scores"]["a"] >= 10

def test_run_projeto_com_status_invalido_falha_alto(tmp_path):
    proj = _mk(tmp_path)
    v = json.loads((proj / "dados" / "verdicts.json").read_text())
    v[0]["status"] = "INVENTADO"
    (proj / "dados" / "verdicts.json").write_text(json.dumps(v), encoding="utf-8")
    rep = run_project(str(proj))
    assert rep["contract_ok"] is False
    assert "INVENTADO" in rep["contract_error"]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m pytest tests/test_run.py -q`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: Write minimal implementation**

```python
# engine/run.py
"""Runner do beta-tester: roda a cadeia determinística sobre projetos/NN/ e reporta.
Uso: python3 -m engine.run projetos/02-simulador-caixa-split"""
import json
import os
import sys
from engine.contract import validate_claims, validate_verdicts, ContractError
from engine.score import score as score_claims  # ajuste ao nome real exportado por score.py


def _load(path):
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def run_project(proj_dir):
    dados = os.path.join(proj_dir, "dados")
    claims = _load(os.path.join(dados, "claims.json"))
    verdicts = _load(os.path.join(dados, "verdicts.json"))
    rep = {"contract_ok": True, "contract_error": None, "scores": {}}
    try:
        validate_claims(claims)
        validate_verdicts(verdicts)
    except ContractError as e:
        rep["contract_ok"] = False
        rep["contract_error"] = str(e)
        return rep
    rep["scores"] = score_claims(claims, verdicts)
    return rep


if __name__ == "__main__":
    r = run_project(sys.argv[1])
    print(json.dumps(r, ensure_ascii=False, indent=2))
```

**NOTA de execução:** antes do Step 4, abrir `engine/score.py` e confirmar o nome da função pública que recebe `(claims, verdicts)` e retorna `{id: score}`. O `__main__` de score.py usa argv e grava arquivo; pode ser preciso extrair/expor uma função `score(claims, verdicts) -> dict` (refactor mínimo, sem mudar comportamento) e o `__main__` passa a chamá-la. Se já existir, importar direto.

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m pytest tests/test_run.py -q`
Expected: PASS (2 passed)

- [ ] **Step 5: Modify README.md** — adicionar seção Quickstart:

```markdown
## Quickstart (rodar o estado de um projeto)

    python3 -m engine.run projetos/02-simulador-caixa-split

Reporta: contrato OK?, score de cada claim, bloqueios. Se um agente emitiu JSON
fora do contrato (status inventado, campo faltando), falha-alto aqui com a causa.
```

- [ ] **Step 6: Commit**

```bash
git add engine/run.py tests/test_run.py README.md
git commit -m "feat(run): CLI runner da cadeia determinística + quickstart"
```

---

## Task 5: `tests/test_e2e.py` — cadeia ponta-a-ponta + cicatriz e2e

**Files:**
- Create: `tests/test_e2e.py`

As peças da cicatriz têm unit; a **composição não**. Este teste roda o loop real: assenta pai → desce (filhos limpos) → reabre pai → filhos re-cicatrizam.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_e2e.py
from engine.descent import descent_gate, refresh_provisional, is_resolved

def test_cicatriz_e2e_reabrir_pai_recicatriza_filhos():
    # pai resolvido, filho desceu limpo
    pais_verds = {"pai": {"status": "PASSA"}}
    filhos = [{"id": "f", "parent": "pai"}]
    # 1. pai resolvido -> nenhum provisional
    abertos_antes = [pid for pid in ["pai"] if not is_resolved("pai", pais_verds)]
    assert refresh_provisional(filhos, abertos_antes) == []
    assert "provisional" not in filhos[0]
    # 2. reabre o pai (vira não-resolvido)
    pais_verds["pai"] = {"status": "PRECISA-JUSTIFICAR"}
    abertos_depois = [pid for pid in ["pai"] if not is_resolved("pai", pais_verds)]
    # 3. filho re-cicatriza
    assert refresh_provisional(filhos, abertos_depois) == ["f"]
    assert filhos[0]["provisional"]["state"] == "review"

def test_chain_determinística_nao_quebra():
    # claims -> contrato -> score, fixture mínima
    from engine.contract import validate_claims, validate_verdicts
    claims = [{"id": "a", "content": "c", "binding": "vinculante",
               "provenance": "human decision", "parent": None, "links": []}]
    verds = [{"id": "a", "status": "PASSA", "structural_gaps": [], "verification_items": [],
              "elicitation_question": None, "contradiction": None}]
    validate_claims(claims)
    validate_verdicts(verds)  # não levanta
```

**NOTA:** confirmar a assinatura real de `is_resolved` em `engine/descent.py` (Step 0: ler o arquivo). Se ela receber `(claim_id, verdicts)` ajustar; se receber o objeto-verdict, adaptar a chamada. O comportamento esperado (resolvido vs não) é o do gate.

- [ ] **Step 2: Run test to verify it fails (ou passa direto se as assinaturas baterem)**

Run: `python3 -m pytest tests/test_e2e.py -q`
Expected: FAIL se assinatura divergir → ajustar a chamada ao real, não o engine.

- [ ] **Step 3: Ajustar chamadas às assinaturas reais** (sem tocar o engine).

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m pytest tests/test_e2e.py -q`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add tests/test_e2e.py
git commit -m "test(e2e): cadeia determinística + cicatriz end-to-end (reabrir pai)"
```

---

## Task 6: estender contract.py — validar verification_item.candidates

**Files:**
- Modify: `engine/contract.py`
- Modify: `tests/test_contract.py`

Os verdicts reais (projeto 02) têm `candidates` nos verification_items (do cross-graph linking): `[{source_claim, excerpt, confidence}]`. O contrato deve aceitá-los e validar quando presentes.

- [ ] **Step 1: Write the failing test** (append a `tests/test_contract.py`)

```python
def test_verification_item_candidates_valido():
    from engine.contract import validate_verdict
    v = {"id": "x", "status": "PENDENTE-VERIFICAÇÃO", "structural_gaps": [],
         "verification_items": [{"text": "q?", "domain": "lei", "critical": True,
             "candidates": [{"source_claim": "s1", "excerpt": "...", "confidence": 10}]}],
         "elicitation_question": None, "contradiction": None}
    assert validate_verdict(v) == v

def test_candidate_confidence_nao_numerica_falha():
    from engine.contract import validate_verdict, ContractError
    import pytest
    v = {"id": "x", "status": "PENDENTE-VERIFICAÇÃO", "structural_gaps": [],
         "verification_items": [{"text": "q?", "domain": "lei", "critical": True,
             "candidates": [{"source_claim": "s1", "excerpt": "...", "confidence": "alta"}]}],
         "elicitation_question": None, "contradiction": None}
    with pytest.raises(ContractError, match="confidence"):
        validate_verdict(v)
```

- [ ] **Step 2: Run to verify fail**

Run: `python3 -m pytest tests/test_contract.py -q`
Expected: FAIL no `test_candidate_confidence_nao_numerica_falha`

- [ ] **Step 3: Estender `validate_verdict`** — dentro do loop `for it in items:`, após validar critical:

```python
        for cand in it.get("candidates", []):
            _require(isinstance(cand.get("source_claim"), str), f"verdict {cid}: candidate sem source_claim")
            _require(isinstance(cand.get("excerpt"), str), f"verdict {cid}: candidate sem excerpt")
            _require(isinstance(cand.get("confidence"), (int, float)),
                     f"verdict {cid}: candidate confidence deve ser numérica")
```

- [ ] **Step 4: Run to verify pass**

Run: `python3 -m pytest tests/test_contract.py -q`
Expected: PASS (10 passed)

- [ ] **Step 5: Commit**

```bash
git add engine/contract.py tests/test_contract.py
git commit -m "feat(contract): valida candidates (cross-graph) no verification_item"
```

---

## Task 7: `tests/test_embed.py` — único módulo engine sem teste

**Files:**
- Create: `tests/test_embed.py`

- [ ] **Step 0: Ler `engine/embed.py`** (670B) — descobrir a função pública e o que retorna (provável: embedding/similaridade pra retrieval).

- [ ] **Step 1: Write the failing test** — esqueleto a preencher com a assinatura real:

```python
# tests/test_embed.py
from engine import embed

def test_embed_deterministico():
    # mesmo input -> mesmo output (sem rede, sem aleatório)
    a = embed.embed("texto")        # ajuste ao nome real
    b = embed.embed("texto")
    assert a == b

def test_embed_textos_diferentes_diferem():
    assert embed.embed("gato") != embed.embed("avião")
```

**NOTA:** se `embed.py` chama rede/modelo externo, o teste vira: garantir o fallback determinístico OU marcar `@pytest.mark.skip` com motivo e abrir item separado. Não mocke modelo pesado só pra ter verde — teste o que é determinístico.

- [ ] **Step 2–4:** rodar (fail→ajustar→pass) conforme a assinatura real.

- [ ] **Step 5: Commit**

```bash
git add tests/test_embed.py
git commit -m "test(embed): cobre o único módulo engine sem teste"
```

---

## Task 8: `tests/test_lentes.py` — catálogo de lentes do critic

**Files:**
- Create: `tests/test_lentes.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_lentes.py
import csv, pathlib

LENTES = pathlib.Path(__file__).parent.parent / "engine" / "lentes.csv"

def test_lentes_parseia_e_shape():
    rows = list(csv.DictReader(open(LENTES, encoding="utf-8")))
    assert len(rows) >= 6
    cols = {"categoria", "nome", "descricao", "quando_usar"}
    for r in rows:
        assert set(r) == cols
        assert all(v and v.strip() for v in r.values())

def test_lentes_nomes_unicos_e_esperados():
    rows = list(csv.DictReader(open(LENTES, encoding="utf-8")))
    nomes = [r["nome"] for r in rows]
    assert len(nomes) == len(set(nomes))
    assert {"pre-mortem", "red-team", "first-principles"} <= set(nomes)
```

- [ ] **Step 2: Run to verify pass** (lentes.csv já existe)

Run: `python3 -m pytest tests/test_lentes.py -q`
Expected: PASS (2 passed)

- [ ] **Step 3: Commit**

```bash
git add tests/test_lentes.py
git commit -m "test(lentes): catálogo de crítica parseia + shape"
```

---

### Fim da Fase A — gate de beta

- [ ] Rodar tudo: `python3 -m pytest tests/ -q` (esperado: 84 + ~31 novos verdes) e `cd app && node --test` (16 verdes).
- [ ] `python3 -m engine.run projetos/02-simulador-caixa-split` reporta sem erro de contrato.
- [ ] Atualizar memória: beta-1 fechado, listar o que foi implementado.

---

# FASE B — PESQUISA (pós-beta, NÃO inflar o escopo)

> Estes itens são `[Especulativo]` — não estão especificados a ponto de código-completo. **Cada um começa por um design-spike** (decidir o shape COM o usuário) porque escrever a implementação agora seria inventar a decisão — o pecado que o motor mata. Só entram se o usuário fundar escopo explicitamente. Ordem por dependência.

## Task 9: tags de oráculo (`structural | worldly | decision`)
**Spike:** decidir onde a tag vive — campo no claim? derivada do verdict (worldly = tem verification_item; decision = provenance human; structural = resto)? **Provável lazy:** derivada, não armazenada (já tens os 3 sinais espalhados — `provenance`, `verification_items`, `status`). Entregável do spike: 1 função `oracle_of(claim, verdict) -> {structural|worldly|decision}` + teste. Depende de: contract.py (Task 1). É a base conceitual da Task 10.

## Task 10: teste-como-nó-no-grafo (oracle slot / link "valida")
**Spike:** generalizar `integrity.py` (que já força teste-verde) para que o teste seja um **nó com `link` de tipo "valida"** apontando ao nó-código, emitindo veredito no grafo. Fecha o buraco do órfão de vez e realiza a unificação `validate(unidade, oráculo)`. Depende de: Task 9 (a tag diz o oráculo). Risco: god-object — manter o gate-da-ideia separado, só unificar a *interface de veredito*, não os agentes.

## Task 11: assinatura de derivação explícita (`Γ_pai ⊢ filho`)
**Spike:** hoje a regra pai→filho é implícita em `parent_constraint`. Torná-la explícita: cada nível declara a regra que o `expander` aplica e o `gate` confere. Entregável: formato da assinatura + checker determinístico + extensão dos prompts expander/gate. Depende de: nada de código novo, mas é o item mais conceitual — exige o usuário definir as regras por nível.

## Task 12: cross-graph linking completo (README §12)
**Spike:** `linking.py` já existe (`resolve_link`, `link_claim`) e os verdicts já carregam `candidates`. Falta o fluxo do **endereço-de-domínio** no `verification_item` dirigindo a busca cross-graph automática. Entregável: fechar o loop verification_item.domain → busca no source graph daquele domínio → grava candidate. Depende de: Task 6 (contract valida candidates). Menor risco da Fase B — peças quase todas existem.

## Task 13: histórico por-claim + contador global de interação
**Spike:** estender `journal.py` (Task 2) com snapshots da tríade (claim+score+veredito) por rodada — o "git-blame do claim" da memória `sistema-historico-versionamento-claims`. Decisão do spike: snapshot por rodada inteira vs por claim; storage (JSONL append de snapshots). Depende de: Task 2 (journal.py). YAGNI até alguém querer reverter/auditar um claim no tempo.

---

## Self-Review (coberto vs spec)

- contract LLM↔código → Task 1, 6 ✅
- histórico real (decision-log/addendum/contexto) → Task 2 ✅
- meta-analista rodável + floor-plan dado → Task 3 ✅
- entrypoint/quickstart → Task 4 ✅
- e2e + cicatriz e2e → Task 5 ✅
- embed.py → Task 7 ✅ · lentes.csv → Task 8 ✅
- oracle tags → Task 9 · test-como-nó → Task 10 · assinatura derivação → Task 11 · cross-graph → Task 12 · histórico por-claim → Task 13 (todos spike-first, Fase B)

Sem placeholders na Fase A (código completo). Fase B é spike-first por desenho (não é placeholder: é honestidade — a decisão é do usuário, não inventada).
